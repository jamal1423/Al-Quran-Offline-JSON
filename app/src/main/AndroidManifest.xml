<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apps.qurankarim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuranKarim">
        <activity android:name=".KumpulanDoaDetail"></activity>
        <activity android:name=".KumpulanDoa" />
        <activity android:name=".DzikirSetelahSholat" />
        <activity android:name=".DoaSetelahSholat" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SurahDetail" />
        <activity android:name=".MainMenu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>